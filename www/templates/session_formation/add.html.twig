{% extends 'base.html.twig' %}

{% block title %}Ajouter une session de formation{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter une session de formation</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary mt-3">Ajouter</button>
        {{ form_end(form) }}
        <a href="{{ path('app_session_formation') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
