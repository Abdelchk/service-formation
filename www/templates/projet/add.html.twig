{% extends 'base.html.twig' %}

{% block title %}Ajouter un projet{% endblock %}

{% block body %}
    <h1>Ajouter un projet</h1>

    {{ form_start(form) }}
        {{ form_row(form.Nom) }}
        {{ form_row(form.BudgetInitial) }}
        {{ form_row(form.SeuilAlerte) }}
        {{ form_row(form.IdClient) }}
        {{ form_row(form.FormateurReferent) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_projet') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
